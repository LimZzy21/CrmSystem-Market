## Angular CRM+Market app 
  `npm start / ng serve`
## Run DB
`npx json-server data/db.json`
(localhost:3000) for check all DB items (you also can change port, use --port ****)
## Used npm packages
[chart.js, json-server, npx-pagination]
